function displayImage() {
    // Get the callout element
    var callout = document.getElementById("callout");

    // Display the callout with the image
    callout.style.display = "block";
}

function hideImage() {
    // Get the callout element
    var callout = document.getElementById("callout");

    // Hide the callout
    callout.style.display = "none";
}